import React from 'react';
import ReactDOM from 'react-dom';


ReactDOM.render(
    <React.StrictMode>
        <div>
            <span>hello,Simple docker!</span>
            <br/>
            <span>I'm reading!</span>
        </div>
    </React.StrictMode>,
    document.getElementById('root')
);

